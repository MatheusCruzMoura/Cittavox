import styled from "styled-components";

const HomeMenu1Item = styled.a `
    width: 167px;
    height: 151px;
    border: 1.5px solid var(--azul);
    border-radius: var(--botao);
    text-decoration: none;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-evenly;

    img {
        width: 50px;
        height: 50px;
    }
`

export default HomeMenu1Item
