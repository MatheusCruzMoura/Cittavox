import styled from 'styled-components'
import Roboto1 from '../Roboto1'

const RobotoPerfil = styled.a`
font-family: var(--roboto);
font-weight: var(--semibold);
color: var(--cinza5);
font-size: 18px;
margin-top:43px;

cursor: pointer;

letter-spacing: -0.03em;
text-decoration-line: underline;    
`

export default RobotoPerfil

