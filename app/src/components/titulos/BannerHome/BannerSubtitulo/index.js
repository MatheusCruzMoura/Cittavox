import styled from "styled-components";

const BannerSubtitulo = styled.h2 `
    font-family: var(--nunito);
    font-weight: var(--medium);
    color: var(--cinza);
    font-size: 13px;
`

export default BannerSubtitulo
